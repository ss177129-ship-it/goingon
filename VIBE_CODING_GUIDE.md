# 바이브코딩 가이드 — 비전공 1인 개발자를 위한 판단 기준

클로드코드로 개발할 때 방향과 우선순위를 결정하는 데 필요한 지식. 코드를 읽는 법이 아니라, **코드를 못 읽어도 좋은 결정을 내리는 법**을 담는다.

## 1. 검증이 곧 나의 코드 리뷰다

코드를 못 읽는다는 건 약점이 아니라 전제 조건이다. 대신 검증을 기계에 맡긴다.

- AI의 "완료했습니다"는 주장이지 증거가 아니다. 증거는 `flutter analyze` 통과, 테스트 통과, 스크린샷, 실기기 동작 네 가지뿐
- AI가 뭔가 끝냈다고 하면 "어떻게 확인했어? 증거를 보여줘"라고 물을 권리가 항상 있다
- 위험 신호: 테스트를 "일단 건너뛰자"고 함, 보안 규칙을 풀어서 에러를 "해결"함, 확인 없이 "될 겁니다"라고 함. 셋 다 CLAUDE.md 금지 사항에 있으니 지적하면 됨

## 2. 작게 시키고, 자주 새로 시작하라

- 한 번에 한 가지만 시킨다. "설정 탭 만들고 버그도 고치고 디자인도 정리해줘"는 셋 다 어중간해진다. TODO.md에 번호가 있는 이유
- 대화가 길어지면 AI 성능이 떨어진다. 한 과제가 끝나면 `/clear`로 새 대화를 시작하는 게 좋다 — CLAUDE.md가 있으니 규칙은 잊히지 않는다
- 큰 작업 전엔 "구현하지 말고 계획부터 보여줘"라고 시키면(플랜 모드) 방향이 틀렸을 때 코드를 버리지 않아도 된다

## 3. Git은 세이브 포인트다

- 게임 세이브처럼, **잘 돌아가는 상태마다 커밋**한다. 과제 하나 완료 = 커밋 하나
- 뭔가 크게 망가졌을 때 30분 넘게 디버깅이 안 풀리면, 고치려 하지 말고 마지막 커밋으로 되돌린 뒤 다시 시도하는 게 거의 항상 빠르다
- "지금 상태 커밋해줘. 메시지는 한국어로"라고 시키면 된다. 커밋 안 하고 이틀 작업하는 게 1인 개발 최대 리스크

## 4. 우선순위 결정 프레임: "출시를 막는가?"

막힐 때마다 이 순서로 판단한다:

1. **유저 데이터가 사라지거나 앱이 죽는 문제** — 무조건 최우선 (예: 러닝 기록 유실)
2. **심사 리젝 사유** — 출시 자체를 막음 (권한 문구, 데모 모드, 차단/신고)
3. **핵심 감정을 전달하는 기능** — 고잉온이 팔려는 "함께 달리는 감각" (제스처, 햅틱, 공유 카드)
4. **폴리시** — 간격, 애니메이션, 문구
5. **새 기능** — 출시 전엔 기본적으로 전부 거절. "그거 좋네, v1.1에"가 정답

새 아이디어가 떠오르면 "이게 위 1~4번 중 어디에 해당하지?"를 먼저 묻는다. 5번이면 TODO 맨 아래에 적고 잊는다.

## 5. 되돌리기 어려운 결정 vs 쉬운 결정

생각하는 시간을 어디에 쓸지 정하는 기준. **되돌리기 어려운 결정에만 오래 고민한다.**

- 되돌리기 어려움 (신중하게): Firestore 데이터 구조, 계정/로그인 모델, 보안 규칙 설계, 앱 이름·번들 ID. 이미 유저 데이터가 쌓인 뒤에 바꾸려면 마이그레이션이라는 고통이 따름
- 되돌리기 쉬움 (빨리 결정하고 넘어감): UI 디테일, 문구, 색, 애니메이션, 기능 순서. 틀려도 다음 업데이트에 고치면 됨

AI에게 물을 때도 "이 결정 나중에 바꾸기 어려워?"를 물으면 판단이 빨라진다.

## 6. 기술 부채: 빚을 져도 되는 곳과 안 되는 곳

출시를 위해 대충 넘어가는 것(기술 부채)은 전략이지 죄가 아니다. 단, 빚을 져도 되는 영역이 정해져 있다.

- 빚 OK: 기능 범위 축소(라이브 합산 v1.1로), 정확도 타협(kcal 고정 추정), 수동 프로세스(운영 자동화 없음)
- 빚 금지: 데이터 무결성(기록 유실·이중 집계), 보안(규칙 완화), 복구 경로(앱 죽으면 기록 증발)

요약: **기능은 잘라도 신뢰성은 자르지 않는다.** 유저는 기능 없는 앱은 기다려주지만 기록 날린 앱은 삭제한다.

## 7. 시뮬레이터가 못 잡는 것들

시뮬레이터에서 완벽해도 실기기에서 처음 드러나는 문제들이 있다. GPS 정확도와 드리프트, 백그라운드 동작(잠금·전화 수신·앱 전환), 권한 다이얼로그 실제 흐름, 배터리 소모, 햅틱 감각, 네트워크 불안정. **고잉온은 이 목록 전부에 해당하는 앱이다.** 실기기 테스트를 "마지막 확인"이 아니라 "두 번째 개발 단계"로 생각할 것.

## 8. 비용 걱정은 아직 하지 않는다

Firebase는 유저 수천 명까지 사실상 무료 구간이다. "읽기 횟수 최적화" 같은 조언에 시간 쓰지 말 것 — 그건 유저가 생긴 다음의 행복한 고민이다. 지금 아껴야 하는 유일한 자원은 **출시까지의 시간**이다.

## 9. AI에게 방향을 물을 때의 요령

- 결정을 통째로 맡기지 말고 **선택지와 트레이드오프**를 요구한다: "방법 2~3개와 각각의 장단점, 그리고 추천을 줘"
- 설명이 어려우면 "비전공자에게 설명하듯 비유로"라고 요구한다. 이해 못 한 채 승인하는 게 최악
- AI가 확신에 차 있어도 틀릴 수 있다. 특히 "이게 표준입니다"류 주장은 "고잉온 상황에도 맞아?"라고 되묻는다 — 표준은 대규모 팀 기준인 경우가 많다
- 의도를 말하면 결과가 좋아진다: "버튼 추가해줘"보다 "심사관이 데모를 못 찾으면 리젝되니까 설정에서 눈에 띄게 해줘"

## 10. 출시가 완성이 아니라 시작이다

- 첫 버전의 목표는 완벽이 아니라 **"핵심 감정이 전달되는 최소한"**. 고잉온이라면: 두 사람이 함께 시작하고, 기록이 안 사라지고, 합산이 감동적으로 보이면 끝
- 심사 리젝은 실패가 아니라 정상 과정이다 (1~2회 예상은 현실적 계획)
- 출시 후 진짜 우선순위는 유저가 알려준다. 지금 v1.1 목록(HealthKit, 라이브 합산…)도 첫 유저 10명의 피드백 앞에서는 전부 가설일 뿐
