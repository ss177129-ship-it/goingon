# 기술 지식 노트 — 고잉온 코드로 배우는 프로그래밍 개념

개발 방향과 우선순위를 판단할 때 필요한 기술 개념들. 전부 고잉온 코드에 실제로 존재하는 것들이라, 각 개념마다 "우리 앱 어디에 있는지"를 함께 적는다. AI와 대화할 때 이 단어들을 쓰면 지시의 정확도가 올라간다.

## 1. 클라이언트-서버, 그리고 "백엔드 없음"의 의미

앱(클라이언트)은 유저의 폰에서 돌고, 데이터는 서버(Firestore)에 있다. 고잉온은 자체 서버가 없는 **서버리스** 구조 — 모든 로직이 폰에서 실행되고, Firebase가 서버 역할을 대신한다.

- 의미 1: 폰에서 도는 코드는 **유저가 조작할 수 있다.** 악의적 유저가 앱을 뜯어 가짜 거리를 올리는 걸 코드로는 못 막는다 → 그래서 보안 규칙(7번)이 유일한 방어선
- 의미 2: **서버에서 주기적으로 도는 작업이 불가능하다.** "매일 자정에 좀비 세션 정리" 같은 걸 못 함 → 그래서 클라이언트가 읽을 때 걸러내는 방식을 씀 (`finishedSessionsWith`의 24시간 기준)

## 2. 비동기(async/await)와 Future

네트워크 요청은 시간이 걸린다(수십 ms~수 초). 그동안 앱이 멈추면 안 되니까, "결과가 나중에 온다"는 약속(`Future`)을 받고 다른 일을 계속한다. `await`는 "여기서 결과를 기다렸다가 다음 줄로"라는 뜻.

- 고잉온: 서비스 함수 전부 (`await RunService().submitResult(...)`)
- 판단 포인트: `await`가 있는 줄은 **실패할 수 있는 줄**이다. 네트워크가 끊기면 여기서 에러가 난다. "이거 실패하면 유저가 뭘 보게 돼?"를 물어야 하는 지점

## 3. 스트림(Stream)과 리스너 — 실시간의 정체

Future가 "한 번 받고 끝"이라면 Stream은 "구독해두면 바뀔 때마다 계속 옴". 상대가 GO?를 보내면 내 홈 화면이 즉시 반응하는 건, 홈이 `incomingSessions` 스트림을 구독하고 있어서다.

- 고잉온: `sessionStream`(로비·러닝 중 세션 변화 감지), `friendsStream`(친구 목록), `authStateChanges`(로그인 상태)
- 판단 포인트: 구독은 **끊지 않으면 계속 돈다** — 배터리·비용·메모리를 먹는다. 화면이 사라질 때 `dispose()`에서 구독을 취소하는 게 규칙. "리스너 정리했어?"는 유효한 리뷰 질문

## 4. 생명주기(lifecycle) — 앱과 화면은 태어나고 죽는다

화면(위젯)은 `initState`(생성) → 사용 → `dispose`(소멸)를 거친다. 앱 전체도 포그라운드 → 백그라운드 → (iOS가 임의로) 강제 종료의 생명주기가 있다.

- 고잉온의 핵심 사건 두 개가 전부 여기서 나왔다:
  - iOS는 백그라운드에서 Timer를 멈춘다 → 경과 시간을 타임스탬프 차이로 계산하는 이유
  - iOS는 백그라운드 앱을 예고 없이 죽인다 → `run_recovery.dart`가 존재하는 이유
- 판단 포인트: 모바일 개발 버그의 절반은 "앱이 잠들거나 죽었을 때"에서 나온다. 새 기능마다 "백그라운드에서는? 강제 종료되면?"을 물을 것

## 5. 경쟁 상태(race condition) — 동시에 쓰면 데이터가 깨진다

두 주체가 같은 데이터를 거의 동시에 읽고-고치고-쓰면, 나중에 쓴 쪽이 먼저 쓴 쪽을 덮어쓴다. 고잉온은 **항상 두 명이 동시에 행동하는 앱**이라 이 문제가 구조적으로 따라다닌다.

- 실제 사례: 둘 다 준비완료 → 양쪽 클라이언트가 동시에 `startRun` 호출 → startedAt이 두 번 찍히면 "함께 출발한 시각"이 밀림 → 먼저 찍힌 값을 유지하는 코드가 있는 이유
- 실제 사례 2: 양쪽이 거의 동시에 결과 제출 → 서로 "상대 결과 없네"라고 판단하면 세션이 영영 finished가 안 됨 → `submitResult`가 트랜잭션인 이유

## 6. 트랜잭션(transaction) — 전부 되거나 전부 안 되거나

여러 개의 읽기/쓰기를 하나의 원자적(atomic) 묶음으로 실행하는 것. 중간에 충돌하면 자동으로 처음부터 재시도된다.

- 고잉온: `startRun`, `submitResult`, `_bumpMonthlyStats`, `cancelSession` 전부 트랜잭션
- 판단 포인트: "A를 지우고 B를 만든다"류 작업(예: username 변경)은 중간에 실패하면 A만 지워진 반쪽 상태가 남는다. **두 개 이상의 쓰기가 논리적으로 한 몸이면 트랜잭션** — 이게 CLAUDE.md에 username 규칙이 있는 이유

## 7. 보안 규칙 — 신뢰할 수 없는 클라이언트 시대의 방어선

Firestore 보안 규칙은 "누가 어떤 문서를 읽고 쓸 수 있는가"를 서버 쪽에서 강제한다. 앱 코드는 조작될 수 있지만 규칙은 조작 불가능 — **규칙이 곧 서버다.**

- 고잉온: 세션 필드 허용 목록(자기 uid 항목만 쓰기 가능), 친구 추가 "+최대 1명" 검사
- 판단 포인트: 권한 에러가 나면 규칙이 아니라 쿼리가 틀렸을 확률이 높다(CLAUDE.md 금지 사항). 그리고 **새 필드를 쓰는 기능 = 규칙 수정이 세트**라는 걸 기억할 것

## 8. 멱등성(idempotency) — 두 번 실행해도 한 번 실행한 것과 같게

모바일은 재시도의 세계다(네트워크 끊김 → 다시 시도). 같은 요청이 두 번 가도 결과가 안 망가지게 설계하는 성질을 멱등성이라 한다.

- 고잉온: 결과 재제출해도 월간 거리가 이중 집계되지 않음(`isFirstSubmit`), 친구 재추가해도 에러 안 남(arrayUnion no-op 허용), GO? 연타해도 세션 하나만 생김(재사용)
- 판단 포인트: 새 기능 리뷰 때 "유저가 버튼을 두 번 누르면? 재시도되면?"을 묻는 습관. 결제·집계·카운트 증가가 특히 위험 지대

## 9. NoSQL 데이터 모델링 — 미리 계산해서 저장한다

Firestore는 표(table)가 아니라 문서(document) 저장소다. 관계형 DB처럼 "조인해서 합산"이 안 되므로, **읽을 때 계산하는 대신 쓸 때 미리 계산해 둔다**(비정규화).

- 고잉온: `monthKm`, `totalRuns`, `weekStreak`를 세션에서 매번 합산하지 않고 `users` 문서에 집계 필드로 유지. `usernames/{username}` 컬렉션도 같은 이유(아이디 중복 방지 + 빠른 검색을 위한 역방향 인덱스)
- 판단 포인트: 집계 필드는 빠르고 싸지만 **원본과 어긋날 수 있다**(이중 집계, 누락). 그래서 갱신 경로를 하나(`_bumpMonthlyStats`)로 강제하는 것. 새 통계가 필요할 때 "읽을 때 계산 vs 쓸 때 집계"는 항상 트레이드오프 결정

## 10. 로컬 저장소 vs 원격 저장소

폰 안(SharedPreferences)은 빠르고 오프라인에도 되지만 앱 삭제 시 사라지고 그 기기에만 있다. 서버(Firestore)는 느리고 네트워크가 필요하지만 영구적이고 어디서나 접근된다.

- 고잉온: 러닝 스냅샷(로컬 — 빠른 주기 저장용), 알림 토글(로컬 — 서버에 있을 필요 없음), 러닝 결과·친구(원격 — 진짜 데이터)
- 판단 포인트: "이 데이터 폰이 바뀌어도 살아야 해?" → 원격. "네트워크 없어도 즉시 필요해?" → 로컬. 둘 다면 로컬에 쓰고 원격에 동기화(러닝 복구가 정확히 이 패턴)

## 11. 에러 처리와 관측성(observability)

에러는 없앨 수 없고 **분류**할 수 있을 뿐이다: 치명적(앱이 죽음) vs 비치명적(기능 하나 실패). 출시 후엔 유저의 폰을 볼 수 없으므로, 에러를 자동 수집하는 장치(Crashlytics)가 개발자의 눈이 된다.

- 고잉온: 삼중 캐치(main.dart) + 서비스 호출마다 `recordError(fatal: false)` + 유저 안내 문구
- 판단 포인트: 좋은 에러 처리의 기준은 "유저가 다음에 뭘 해야 하는지 아는가". 조용한 실패(제스처 전송)와 시끄러운 실패(결과 저장 → 재시도 안내)를 구분한 게 좋은 예

## 12. 의존성(dependency)과 버전

`pubspec.yaml`의 패키지들은 남의 코드다. 공짜로 기능을 얻는 대신 그들의 버그·업데이트·중단에 노출된다. `pubspec.lock`이 정확한 버전을 고정해서 "내 폰에선 됐는데" 문제를 막는다.

- 고잉온: geolocator, share_plus, wakelock_plus 등 — 전부 "직접 구현하면 몇 주 걸릴 것"들
- 판단 포인트: 패키지 추가는 공짜가 아니다(앱 크기, 심사 변수, 유지보수). "이거 패키지 없이 몇 줄이면 돼?"는 유효한 질문. 반대로 GPS·결제처럼 어려운 영역은 검증된 패키지가 정답
- `withOpacity` → `withValues` 같은 **deprecation**(구식화)은 "지금은 되지만 곧 사라질 API"라는 예고 — 미루면 언젠가 업그레이드가 막힌다

## 13. 테스트의 종류와 쓸모

- **유닛 테스트**: 순수 로직 검증(빠름, 안정적) — `week_key_test`, `location_service_test`가 이것. 시간 계산·필터처럼 "틀려도 눈에 안 보이는" 로직에 최고
- **위젯 테스트**: 화면이 그려지고 반응하는지
- **통합 테스트**: 실제 흐름 전체 (고잉온에선 시뮬레이터 수동 테스트가 이 역할)
- 판단 포인트: 전부 테스트하는 건 1인 개발에 비현실적. **"버그가 나도 눈에 안 보이는 곳"**(날짜 경계, 집계, 상태 전이)에만 유닛 테스트를 집중하는 게 가성비 전략

## 14. 상태(state)와 단일 진실 원천(single source of truth)

같은 데이터가 두 곳에 있으면 언젠가 어긋난다. "이 값의 주인이 누구인가"를 정하는 게 상태 설계의 전부다.

- 고잉온: 세션의 진실은 Firestore 문서 하나(양쪽 클라이언트는 그걸 구독할 뿐), 디자인의 진실은 prototype_v2.html, 경과 시간의 진실은 startedAt 타임스탬프(화면의 숫자는 파생물)
- 판단 포인트: 버그 제보의 상당수는 "두 곳의 값이 다르다"다. 그때 물을 것: "이 값의 원천이 어디야? 왜 두 곳에 있어?"
