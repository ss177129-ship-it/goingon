# GoingOn 기술 진단 보고서

**진단일**: 2026-07-12
**진단 범위**: `lib/` 전체, `pubspec.yaml`, iOS `Info.plist`, `firebase.json` (⚠️ `firestore.rules` 파일은 이 저장소에 없음 — 아래 5번 항목 참고)
**진단 방식**: 코드 정적 분석 + `flutter analyze` 실행. 실제 기기/시뮬레이터 실행이나 코드 수정은 하지 않았습니다. 이번 진단은 "지금 건물이 어떤 상태인지" 확인만 하는 단계이고, 보수 공사는 다음 세션에서 우선순위를 정한 뒤 진행합니다.

등급 기준: 🔴 빨강 = 출시 전 반드시 고쳐야 함 (유저가 겪거나 심사 탈락 사유) · 🟡 노랑 = 지금은 괜찮지만 사용자가 늘면 문제 · 🟢 초록 = 잘 되어 있는 부분

---

## 1. 코드 품질

### 🟢 컴파일 에러 없음 — 골조는 튼튼함
`flutter analyze`를 앱 코드(`lib/`)에 돌려본 결과 87건 중 진짜 오류는 0건이었어요. 나머지는 전부 "정보(info)" 등급 경고예요.
- 비유: 건물 검사에서 안전 위반은 없고, 페인트가 살짝 바랜 곳만 지적받은 상태예요.
- 방치 시: 없음. 다만 아래 두 가지는 손봐두면 좋아요.

### 🟡 `withOpacity` 사용 — 곧 없어질 예정인 API (57곳)
Flutter가 최근 `.withOpacity()`를 구식으로 표시하고 `.withValues()`로 바꾸라고 안내하고 있어요 (색상에 투명도를 줄 때 미세한 계산 오차가 있다는 이유).
- 비유: 지금은 잘 작동하는 옛날 부품인데, 제조사가 "다음 버전부터는 안 만들 거예요"라고 예고한 상태예요.
- 방치 시: 지금 당장은 아무 문제 없어요. 다만 Flutter를 몇 버전 업그레이드하면 언젠가 진짜 삭제(error)로 바뀔 수 있어요.
- 수리 예상 시간: 전체 일괄 치환 15분 (기계적 치환이라 위험 낮음).

### 🟡 테스트 파일이 고장난 채 방치됨
`test/widget_test.dart`가 Flutter 프로젝트 생성 시 기본으로 딸려오는 "카운터 앱" 템플릿 테스트 그대로예요. 존재하지 않는 `MyApp` 클래스(실제 앱 이름은 `GoingOnApp`)를 참조해서, `flutter test`를 돌리면 즉시 실패해요.
- 비유: 건물에 화재경보기가 설치는 되어 있는데, 배선이 애초에 다른 건물 도면대로 연결되어 있어서 눌러도 안 울리는 상태예요.
- 방치 시: 지금도 사용자에게 영향은 없어요(테스트는 개발자만 씀). 다만 "테스트가 있다"는 안전망이 사실은 없는 거라, 나중에 코드가 커지면 사소한 실수를 못 잡아내요.
- 수리 예상 시간: 일단 삭제하거나 앱이 뜨는지만 확인하는 최소 테스트로 교체 — 10분.

### 🟢 리스너/타이머 정리(dispose)가 대체로 꼼꼼함
`run_screen.dart`, `lobby_screen.dart`, `main.dart`의 애니메이션 컨트롤러·타이머·Firestore 구독은 `dispose()`에서 잘 정리되고 있어요. 화면을 나갈 때 `mounted` 체크도 꼬박꼬박 하고 있고요.
- 비유: 방을 나갈 때 불 끄고 가스밸브 잠그는 습관이 대부분의 방에서 잘 지켜지고 있어요.
- 예외 2곳은 아래 "안정성" 섹션에서 별도로 다뤄요.

---

## 2. 성능 (불필요한 rebuild, 리스트 렌더링)

### 🟢 탭 전환 구조는 합리적
`root_screen.dart`가 `IndexedStack`으로 홈/우리/설정 세 탭을 관리해서, 탭을 왔다 갔다 해도 화면이 처음부터 다시 만들어지지 않아요. 좋은 선택이에요.

### 🟡 '우리' 탭이 친구 목록 하나 때문에 두 번 구독됨
`friendsStream`(내 친구 목록을 실시간으로 듣는 기능)이 홈 화면과 '우리' 화면에서 각각 따로 구독돼요. `IndexedStack` 덕분에 두 화면이 동시에 살아있기 때문에, 사실상 똑같은 데이터를 두 번 듣고 있는 셈이에요.
- 비유: 같은 신문을 집에 두 부씩 구독 신청해놓은 것과 비슷해요. 내용은 똑같은데 값은 두 배로 나가요.
- 방치 시: 지금은 사용자가 적어서 티가 안 나지만, 아래 "Firebase 비용" 섹션의 계산에 그대로 반영되는 문제예요.
- 수리 예상 시간: 친구 목록을 상위(RootScreen)에서 한 번만 구독해서 두 화면에 내려주는 구조로 바꾸기 — 1시간.

### 🟢 리스트는 짧아서 당장 문제 없음
'우리' 탭의 "함께한 순간" 리스트는 일반 `Column`으로 그리고 있어요(가상 스크롤이 아닌 방식). 지금처럼 커플당 세션이 수십~백 개 수준이면 괜찮지만, 수천 개가 쌓이면 스크롤이 느려질 수 있어요. 지금 단계에서는 신경 안 써도 되는 수준이라 초록으로 분류했어요.

---

## 3. 배터리 / GPS

### 🔴 GPS 정확도가 "최고 등급(내비게이션용)"으로 설정되어 있어 배터리를 과하게 씀
`location_service.dart`에서 `LocationAccuracy.bestForNavigation`을 쓰고 있어요. 이건 애플이 "카카오내비 같은 실시간 길안내 앱에서만 써라"고 권장하는 가장 전력 소모가 큰 등급이에요. 일반적인 러닝 앱(나이키런, 런데이 등)은 한 단계 아래인 `high`나 `best`를 써요.
- 비유: 방 안 조명을 켜면 되는데, 야구장 조명탑을 켜놓고 조깅하는 셈이에요. 밝기(정확도)는 크게 다르지 않은데 전기세만 훨씬 많이 나와요.
- 예상 소모량: 정확한 실측은 아니지만, 업계 통상치로 볼 때 일반 정확도 설정은 1시간 러닝에 배터리 8~12% 정도를 쓰는 반면, 지금 설정(bestForNavigation)은 15~25% 수준까지 올라갈 수 있어요. 1시간 러닝 후 배터리가 유난히 빨리 닳는다는 사용자 후기가 나올 수 있는 수준이에요.
- 방치 시: 사용자가 매번 러닝 후 "배터리가 너무 빨리 닳는다"고 느낄 가능성이 커요. 특히 오래된 아이폰일수록 체감이 심해요.
- 수리 예상 시간: 설정값 한 줄 교체 — 15분. (실기기로 배터리 % 비교 테스트까지 하면 +1시간)

### 🟢 튐 방지 로직은 잘 되어 있음
GPS가 순간적으로 엉뚱한 곳을 찍는 "GPS 튐" 현상에 대비해 정확도 30m 이상 신호는 버리고, 1초에 50m 이상 이동한 것도 무시하는 방어 코드가 들어가 있어요. 터널·건물 사이·신호 약한 곳에서 거리가 이상하게 튀는 걸 막아주는 좋은 디테일이에요.

### 🟢 배터리 절약을 위한 거리 필터도 적절
5m 이동마다만 갱신하도록 되어 있어요. 이 자체는 배터리를 아끼려는 합리적인 설정이에요 (문제는 위의 정확도 등급이지, 이 필터 값은 아니에요).

---

## 4. Firebase 효율/비용

### 🔴 '우리' 탭을 열 때마다 지금까지의 모든 함께 달린 기록을 통째로 다시 읽음
`run_service.dart`의 `finishedSessionsWith()`는 페이지 나누기(pagination)나 개수 제한(limit) 없이, 두 사람이 지금까지 함께 끝낸 세션을 **전부** 가져와요. 그리고 이게 `friendsStream`이 갱신될 때마다(대부분 앱을 새로 켤 때마다) 다시 실행돼요.
- 비유: 은행 잔고를 확인하고 싶은데, 지금까지의 모든 입출금 내역서를 처음부터 끝까지 매번 새로 인쇄해서 보는 것과 같아요. 거래가 쌓일수록 인쇄비(읽기 비용)가 계속 늘어나요.
- 방치 시: 지금은 커플당 기록이 적어서(수십 건) 괜찮지만, 자주 달리는 커플일수록 이 비용이 눈덩이처럼 불어나요. 아래 계산 참고.
- 수리 예상 시간: 합산 통계(총 거리, 총 횟수 등)를 세션마다 미리 계산해서 `users/{uid}` 문서에 저장해두는 방식(집계 필드)으로 바꾸기 — 2~4시간. 구조를 바꾸는 작업이라 가장 오래 걸리는 항목이에요.

### 무료 한도 초과 시점 추정 (가정 기반 계산 — 실측 아님)

가정: 일일 접속자 1,000명, 앱을 하루 2번 켬, 1인당 친구(파트너) 1명, Firestore 무료 한도는 하루 50,000회 읽기.

| 커플당 누적 "함께 달린 기록" 수 | 하루 총 읽기 수 (1,000명 기준) | 무료 한도(50,000) 대비 |
|---|---|---|
| 5개 (막 시작한 커플) | 약 20,000회 | 여유 있음 |
| **약 20개 (한두 달 꾸준히 달린 커플)** | **약 50,000회** | **딱 한도선** |
| 50개 (석 달 이상 활발한 커플) | 약 110,000회 | 2배 초과 |
| 150개 (반년 이상 매일 러닝) | 약 310,000회 | 6배 초과 |

즉, 평균 사용자가 함께 달린 기록이 **약 20개(대략 한두 달 분량)** 쌓이는 시점에 이미 무료 한도에 도달하고, 그 이후로는 계속 유료 과금 구간이에요. 초과분은 Firestore 기준 읽기 10만 회당 약 0.036달러 수준이라 비용 자체는 크지 않지만(월 몇 달러 수준), "쓰지 않아도 될 돈"이라는 게 문제예요. 위의 집계 필드 구조로 바꾸면 이 비용은 사실상 0에 가깝게 줄어들어요.

### 🟡 친구 목록 스트림이 2배로 구독됨 (성능 섹션과 동일 원인)
위 성능 섹션의 "홈+우리 탭 이중 구독" 문제가 비용에도 그대로 반영돼요. 이것만 고쳐도 읽기 수가 절반으로 줄어요.

### 🟢 쓰기(write) 비용은 문제 없음
세션 생성·준비완료·시작·결과제출을 다 합쳐도 러닝 1회당 쓰기는 6~8회 정도예요. 1,000명이 매일 300세션을 뛴다고 가정해도 하루 2천 회 남짓이라, 무료 한도(하루 2만 회) 안에 충분히 들어와요.

---

## 5. 안정성 (네트워크/크래시/동시성)

### 🔴 Firebase 호출에 실패 처리(try-catch)가 거의 없음 — 사용자가 화면에 갇힐 수 있음
`friend_service.dart`, `run_service.dart`, `auth_service.dart` 안의 Firestore 호출 어디에도 예외 처리나 시간제한(timeout)이 없어요. 특히:
- `run_screen.dart`의 러닝 종료(`_finish`) → 네트워크가 잠깐 끊긴 순간 종료 버튼을 누르면, 결과 저장이 실패해도 사용자에게 아무 안내가 없이 그냥 화면이 멈춰요. 러닝을 다 뛰고도 기록이 못 남고 다음 화면으로도 못 넘어가는 최악의 시나리오예요.
- `invite_screen.dart`의 코드 입력(`_enterCode`) → 실패하면 로딩 스피너가 영원히 도는 상태로 멈춰요.
- 비유: 택배를 보냈는데 배송 실패 알림이 아예 안 오는 거예요. 보낸 사람은 "잘 갔겠지"하고 있는데, 사실은 창고에서 멈춰 있는 상태예요.
- 방치 시: 지하철·엘리베이터 등 통신이 잠깐 끊기는 흔한 상황에서 사용자가 "앱이 멈췄다"고 느끼고 이탈해요. 이 앱의 핵심 순간(러닝 완료)에서 벌어질 수 있다는 게 특히 뼈아파요.
- 수리 예상 시간: 주요 호출 지점(러닝 종료, GO 보내기, 코드 입력, 이름 변경, 탈퇴)에 try-catch + 실패 안내 문구 추가 — 1~2시간.

### 🔴 GPS 스트림에 오류 처리가 없어 크래시 가능성 있음
`location_service.dart`의 `Geolocator.getPositionStream(...).listen(...)`에 오류 콜백(`onError`)이 없어요. 위치 서비스가 시스템 설정에서 꺼져 있거나, 러닝 도중 사용자가 위치 권한을 취소하면 이 스트림이 에러를 던지는데, 받아줄 곳이 없어서 처리되지 않은 예외가 돼요.
- 비유: 소방 대피 계획은 있는데, 정작 화재 감지 센서 자체가 오작동해도 아무 경보도 안 울리게 설계되어 있는 셈이에요.
- 방치 시: 심사관이 시뮬레이터로 테스트하거나(시뮬레이터는 실제 GPS가 불안정할 수 있음), 실기기에서 러닝 도중 위치 권한을 껐다 켤 때 앱이 예기치 않게 멈추거나 튕길 수 있어요. Apple 심사에서 크래시는 거의 즉시 리젝 사유예요.
- 수리 예상 시간: `onError` 콜백 추가 + 권한 취소 시 안전하게 안내 화면으로 전환 — 30분~1시간.

### 🟡 완료 화면(finish_screen)의 실시간 리스너가 정리되지 않음
`finish_screen.dart`는 상대방 결과를 실시간으로 받으려고 `sessionStream(...).listen(...)`을 걸어두는데, 구독을 변수에 저장하지도, `dispose()`에서 끊지도 않아요. (참고로 `dispose()` 메서드 자체가 이 화면엔 아예 없어요.)
- 비유: 손님이 다 나간 방인데, 방문 카메라를 계속 켜놓고 클라우드에 계속 업로드하고 있는 상태예요. 누가 보는 사람도 없는데 데이터 요금만 계속 나가요.
- 방치 시: 사용자가 완료 화면을 스치듯 지나가도 그 세션에 대한 구독이 앱이 완전히 꺼질 때까지 계속 살아있어요. 사용자가 러닝을 많이 할수록 계속 쌓여서, 메모리와 Firestore 읽기 비용이 조금씩 새어나가요.
- 수리 예상 시간: 구독을 변수에 저장하고 `dispose()`에서 취소 — 10분.

### 🟡 두 사람이 동시에 "GO?"를 보내면 세션이 꼬일 수 있음
현재 구조에서는 A가 B에게 GO?를 보내는 것과 B가 A에게 GO?를 보내는 게 완전히 별개의 세션 문서로 생성돼요. 만약 두 사람이 거의 동시에 서로에게 GO?를 보내면, 세션이 2개 만들어지고 각자 다른 세션을 수락해서 서로 다른 "로비"에 들어가 버릴 수 있어요 — 그러면 어느 쪽도 "둘 다 준비완료"가 되지 않아 영원히 상대를 기다리는 상태가 돼요.
- 비유: 친구랑 만날 약속을 잡는데, 서로 동시에 "저기서 만나자"고 다른 장소를 문자로 보내고, 각자 자기가 보낸 장소로 가서 기다리는 상황과 비슷해요.
- 방치 시: 자주 일어나는 일은 아니지만(정확히 동시에 눌러야 함), 발생하면 사용자 입장에서는 "앱이 먹통이다"로 느껴지는 답답한 버그예요. 커플이 함께 쓰는 앱 특성상 두 사람이 동시에 앱을 만지는 상황이 오히려 흔할 수 있어요.
- 수리 예상 시간: 이미 나에게 온 대기 세션이 있으면 새로 만들지 않고 기존 세션으로 합류시키는 로직 추가 — 1~2시간.

### 🟢 회원탈퇴 로직은 꼼꼼함
탈퇴 시 내 계정 삭제뿐 아니라 모든 친구의 목록에서 나를 지우고, 초대 코드도 회수하는 것까지 배치(batch)로 깔끔하게 처리돼요. 실패 시 사용자에게 안내 메시지도 있어요(설정 화면에 try-catch가 적용된 유일한 곳이에요 — 이 부분은 잘 되어 있어요).

---

## 6. 보안

### 🔴 친구 초대가 "상대 동의" 없이 이루어지고, 초대 코드를 무차별 대입할 수 있음
`friend_service.dart`의 `addFriendByCode`를 보면, 코드만 알면 상대방에게 아무 알림이나 승인 절차 없이 곧바로 "친구"로 등록돼요. 게다가 초대 코드는 `GO-XXXX` 형태 4자리(혼동 문자 제외 33개 문자)라 조합 가능한 경우의 수가 약 119만 개예요. 자동화된 스크립트가 코드를 순서대로 시도하면 존재하는 코드를 찾아낼 수 있고, 그 즉시 아무 동의 없이 그 사람의 "친구"가 될 수 있어요.
- 비유: 우리 집 초인종 비밀번호가 4자리인데, 누르면 확인 절차 없이 바로 문이 열리고 그 사람이 우리 집 가족 명단에 자동으로 등록되는 것과 같아요.
- 왜 더 중요한가: 이 앱의 핵심 가치는 "기록이 아니라 관계"예요. 낯선 사람이 동의 없이 내 친구 목록에 들어올 수 있다는 건 이 앱의 신뢰 기반 자체를 흔드는 문제예요.
- 참고: 이 코드 구조가 성립하려면 Firestore 보안 규칙(`firestore.rules`)이 "다른 사용자의 문서 중 `friends` 필드는 누구나 수정 가능"하게 열려 있어야 해요. 그런데 이 파일이 현재 저장소(git)에 없어서, 실제 규칙이 얼마나 넓게 열려 있는지는 콘솔에서 직접 확인이 필요해요 (CLAUDE.md에도 "콘솔에 게시된 버전과 동기화 유지"라고 적혀 있는데, 실제로는 로컬에 사본이 없는 상태예요).
- 방치 시: 악의적인 사용자가 코드를 긁어모아 무작위로 사람들 친구 목록에 끼어들거나, 반대로 다른 사람의 친구 관계를 끊어버리는(`arrayRemove`) 것도 이론적으로 가능해요.
- 수리 예상 시간: (1) Firebase 콘솔에서 규칙을 내려받아 레포에 커밋 + 검토 — 30분. (2) 친구 추가에 "상대방 승인" 단계 추가하거나 최소한 코드 시도 횟수를 제한하는 방어 로직 추가 — 2~4시간.

### 🟢 초대 코드 생성 자체는 안전한 방식을 씀
`Random.secure()`(예측 불가능한 암호학적 난수)를 사용하고 있고, 헷갈리기 쉬운 문자(I, O, 0, 1)를 일부러 제외한 것도 사용자 편의를 세심하게 챙긴 부분이에요. 코드 "생성" 방식 자체엔 문제가 없고, 문제는 "검증 없이 바로 승인"되는 흐름 쪽이에요.

### 🟢 세션 데이터 접근 방식은 안전하게 설계됨
CLAUDE.md에 적힌 대로 세션 조회를 `hostId`/`guestId` 직접 비교로 하고 있어서, `participants` 배열에 `in` 조건을 거는 것보다 규칙 작성이 명확하고 예측 가능해요. 좋은 설계 판단이에요.

---

## 7. 앱스토어 심사 리스크

### 🟢 위치 권한 문구가 명확하고 신뢰감 있게 작성됨
`Info.plist`의 `NSLocationWhenInUseUsageDescription`에 "달린 거리를 기록하기 위해 위치 정보가 필요해요. 위치는 달리기 중에만 사용되고, 친구에게 실시간 위치가 공유되지 않아요."라고 되어 있어요. 이건 심사관이 좋아하는 형태의 문구예요 (왜 필요한지 + 무엇을 안 하는지가 둘 다 명시됨).

### 🟢 백그라운드 위치 권한을 요구하지 않음
`Info.plist`에 "항상 허용(Always)" 권한이나 백그라운드 모드가 없어서, 실제로 의도한 대로 "사용 중에만" 권한만 요구하고 있어요. 이건 심사 난이도를 크게 낮추는 좋은 결정이에요 (CLAUDE.md의 의도된 설계와 일치).

### 🔴 5번(GPS 크래시)·5번(에러처리 없음) 항목이 심사 탈락으로 직결될 수 있음
위 안정성 섹션의 두 빨강 항목(네트워크 실패 시 멈춤, GPS 에러 미처리)은 심사관이 실기기로 테스트하다가 그대로 재현될 수 있어요. 심사관은 보통 와이파이가 불안정한 사무실 환경에서 테스트하고, 위치 권한도 이것저것 눌러보며 테스트하는 경우가 많아서, 이 두 가지는 "일반 사용자보다 심사관이 더 잘 만날 확률이 높은" 버그예요. (중복 언급이 아니라, 이 두 항목이 심사 리스크의 핵심이라는 걸 강조하는 거예요.)

### 🟢 데모 모드가 심사관 시나리오를 잘 커버함
혼자서도 전체 플로우(로비→러닝→완료)를 체험할 수 있는 데모 모드가 잘 만들어져 있어요. 심사관은 파트너 없이 혼자 테스트하는 경우가 대부분이라, 이 기능이 없었다면 핵심 기능을 아예 검증 못 해서 리젝될 뻔한 부분이에요. CLAUDE.md에서 "절대 제거 금지"라고 한 이유가 명확히 이해되는 대목이에요.

---

## 8. 의존성

### 🟡 주요 패키지가 전체적으로 메이저 버전 1개씩 뒤처짐
| 패키지 | 현재 | 최신 |
|---|---|---|
| firebase_core | 3.15.2 | 4.11.0 |
| firebase_auth | 5.7.0 | 6.5.4 |
| cloud_firestore | 5.6.12 | 6.6.0 |
| firebase_messaging | 15.2.10 | 16.4.1 |
| geolocator | 13.0.4 | 14.0.3 |
| google_fonts | 6.3.3 | 8.1.0 |
| share_plus | 10.1.4 | 13.2.0 |

- 비유: 스마트폰 OS가 최신 버전보다 한 단계 낮은 정도예요. 당장 위험하진 않지만, 너무 오래 방치하면 업데이트할 때 한꺼번에 많은 걸 손봐야 해서 부담이 커져요.
- 방치 시: 지금 당장은 문제 없어요. 다만 v1.1에서 FCM 푸시(`firebase_messaging`)를 실제로 쓰기 시작할 때는 최신 버전으로 올려두는 게 낫고, 너무 오래 묵히면 나중에 한 번에 여러 버전을 건너뛰며 업그레이드해야 해서 위험이 커져요.
- 수리 예상 시간: 지금 당장 급하지 않음. 출시 후 여유 있을 때 1~2시간 잡고 순차 업그레이드 + 회귀 테스트 권장.

---

## 종합 소견

이 앱은 "겉보기엔 멀쩡한데 몇 가지 배관·배선 공사가 안 끝난 신축 건물" 같은 상태예요. 골조(코드 구조, 화면 설계, 디자인 일치도)는 비전공 1인 개발치고 매우 탄탄하고, 특히 데모 모드나 회원탈퇴 로직처럼 "귀찮지만 중요한" 부분들을 놓치지 않고 챙긴 흔적이 곳곳에 보여요. 다만 네트워크가 끊기거나 GPS에 문제가 생겼을 때를 대비한 안전장치(에러 처리)가 거의 없다는 점, 그리고 친구 추가가 상대 동의 없이 이루어질 수 있다는 점은 각각 "심사 탈락"과 "관계 앱으로서의 신뢰"라는 이 프로젝트의 두 가지 핵심 목표를 직접 위협하는 부분이라 출시 전에 반드시 손봐야 해요. 나머지(배터리, Firebase 비용, 오래된 패키지)는 지금 당장 무너지는 문제는 아니지만, 사용자가 늘고 시간이 지날수록 서서히 무게가 실리는 종류의 부채라 출시 직후 여유 생겼을 때 순서대로 갚아나가면 충분해요.

### 🔴 빨강 항목 수리 순서 제안 (총 5건)

1. **GPS 스트림 오류 처리 없음** — 크래시로 이어질 수 있어 심사 리스크가 가장 직접적. 먼저.
2. **Firebase 호출에 try-catch가 전무함 (특히 러닝 종료)** — 사용자가 직접 겪는 "화면에 갇힘" 버그. 1번과 함께 처리하면 효율적(둘 다 "실패 시 어떻게 할지" 정하는 작업이라 한 번에 묶어서 고치는 게 나아요).
3. **초대 코드 무단 친구추가 + firestore.rules 레포에 없음** — 콘솔에서 규칙부터 확인/커밋하고, 승인 절차나 방어 로직 설계는 다음 세션에서 논의.
4. **GPS 정확도 등급(bestForNavigation → high/best)** — 수리 시간이 가장 짧고(15분) 효과는 큰 항목이라 우선순위에 비해 "가성비"가 제일 좋아요.
5. **finish_screen 리스너 미정리** — 당장 급하진 않지만 수리가 10분이면 끝나는 항목이라 위 네 가지를 손보는 김에 같이 처리하는 걸 추천해요.

노랑 항목들(Firebase 비용 구조, 친구 스트림 중복 구독, 동시 GO? 처리, 오래된 패키지, 고장난 테스트 파일)은 출시를 막는 건 아니니, 출시 후 사용자가 늘기 시작할 때 순서대로 처리하면 돼요.
