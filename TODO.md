# TODO — 작업 과제 (우선순위 순)

## ★ 개편 시퀀스 P1~P8 — 지금의 본류 (2026-08-22 P0에서 등록)

프롬프트 원문은 `docs/redesign/rebuild_prompts.md`. **순서대로, 하나씩, 완료 기준 통과 후 다음으로.**
건너뛰면 옛 설계 위에 새 코드를 쌓게 된다. 각 프롬프트 끝 = 커밋 하나.

- [x] **P0 — 거버넌스 개정** (2026-08-22, CEO 승인) — CLAUDE.md의 '의도된 설계 결정'을 strategy_memo D-001~006에 맞춰 개정. 사운드 단일 기준을 sound_ux_v1.md → product_design_v1.1.md §1로 승계
- [~] **P1 — M0 스파이크 (합성 검증 통과 2026-08-22 · 실측 미완)** — CEO가 실주행 CSV(cadence_probe.csv, tempo_probe.csv)를 가져오면 랩 구간별 수동 카운트 대비 오차 계산. ±3spm 미달이면 `cadence_engine.dart`의 kThresholdK·검출 창 튜닝 후 재실측. 지터 p95>30ms면 SoLoud 오디오 스레드 대안 설계. 통과 시 "실측 확정(날짜)" 주석. **P1 통과 전 P2 금지**
  - [x] 합성 신호 검증 층 — `test/cadence_engine_test.dart`(17개). 알고리즘 결함 2건을 실주행 전에 잡음: 적응 임계의 절대 바닥 누락(정지 시 139spm), gait hold 10초 이중 계수
  - [x] CSV 판정기 — `tools/analyze-probe-csv.py` (케이던스 구간별 오차 / 템포 지터 p95)
  - [ ] **실주행 CSV로 ±3spm 확정** — 주머니·암밴드·손 각 1회, 걷기↔달리기 전환 3회 포함. 이것이 남아 있는 한 파라미터는 가설이다
  - [ ] **실기에서 지터 p95 측정(M0-b)** — 시뮬레이터 값은 iOS 기기를 대표하지 않는다. 넘으면 SoLoud 오디오 스레드로 이관
- [x] **P2 — 공명 엔진 입력 교체: 페이스 → 케이던스** (2026-08-23)
  - 판정 규칙은 `CadenceMatch`(resonance.dart). 선형 사상 1-Δ/30이 기존 문턱 표와 맞물린다 — Δ3 → 0.90(공명 진입 위) · Δ6 → 0.80(이탈) · Δ9 → 0.70(나란히)
  - 진입 8초 / 해제 10초 dwell. **무장은 진입 문턱, 유지는 이탈 문턱** — 둘 다 진입 문턱으로 하면 히스테리시스와 dwell이 서로를 무력화한다(실제로 재현됨)
  - 정수비 폴리리듬(1:2, 2:3). 러닝 대역 전체를 훑는 테스트가 "정수비가 엉뚱한 짝을 공명시키지 않는다"를 지킨다. 대역 밖 값은 0이 아니라 null
  - 입력 소스 추상화: `PartnerCadenceSource` — `LivePartnerCadence`(벽시계·6초 staleness) / `GhostCadenceTimeline`(세션 경과 시간축·delta 압축) / `FirstAvailableCadence`
  - **케이던스 파라미터는 여전히 실측 미검증** — maxGapSpm 30, resonantGapSpm 3은 설계 초기값이고 CadenceEngine도 합성 검증만 통과했다. P1 실주행 후 재검토
- [x] **P3 — 세션 상태머신 + 에피소드 채보** (2026-08-23) — SessionPhase(intro→build→mission→climax→outro→reverb→freeRun/chain/cooldown). reverb 판정은 GaitState가 결정(running 유지 → freeRun 자동, walking 10초 → cooldown). 채보 JSON + ChartPlayer + 에피소드 '기차' 1종. 전화 수신 자동 일시정지→꼭지 재개, 4분 이상 중단 시 부분 완주. run_recovery 경로 유지
  - `SessionController` — 타이머를 갖지 않고 시계를 주입받는다. 8분 시나리오를 밀리초 단위로 시험할 수 있고 백그라운드에서 틱이 밀려도 결과가 같다
  - 입력은 셋뿐: 발(GaitState) · 꼭지(문맥이 뜻을 정함) · 전화(자동 일시정지). **화면은 입력 목록에 없다**
  - 채보는 데이터(`assets/episodes/episode_train.chart.json`). 화자 6문장·스퍼트 30초 상한을 `Chart.validate`가 로딩 시점에 거른다
  - 스템은 기존 pad 에셋 임시 재활용 — 정식 스템 3벌 제작은 M0-b의 귀 판정 후 별도 트랙
  - **남은 것: run_screen 배선은 P5.5로 미뤄졌다.** P5에서 붙은 것은 고스트(기록기·재생기)뿐이고, 세션 상태머신·채보·전화 인터럽션은 아직 화면에 닿지 않았다
- [x] **P4 — 고스트런 3막** (2026-08-23) — 모든 러닝이 고스트가 되도록 저장 스키마에 케이던스 타임라인(1초, delta 압축)+사연 필드. 공개 기본 '페이스메이트만', GPS 경로 기본 비저장. GhostEngine을 P2 인터페이스의 두 번째 입력으로 → 시차 공명. 종료 알림은 서버 발송(functions/push.ts). 승패 언어 금지
  - **`runs/{runId}` 새 컬렉션** — 세션 문서에 넣지 않았다. 혼자 달린 러닝에는 세션이 없어서, 세션 안에 두면 솔로런이 영영 고스트가 되지 못한다(§3-2는 "모든 러닝"을 요구)
  - `GhostRecorder`(1Hz 수집·느려짐 마커) / `GhostEngine`(재생·작별, PartnerCadenceSource 구현) / `GhostService`(저장·조회·함께달림 기록)
  - 보안 규칙: 공개 범위를 규칙이 강제한다(기본 pacemates, 양쪽 친구 목록 확인). **남긴 뒤 바꿀 수 있는 것은 visibility와 story뿐** — 거리·케이던스는 불변
  - `onGhostCompanion` Cloud Function + 인덱스 2개. 하루 3건 넘으면 개별 알림 대신 조용히 쌓임(다이제스트는 P7)
  - 승패 언어 감사: 영문 식별자 0건, 한글 매치는 전부 오탐(우선순위·과거형 어미·금지 주석)
  - 기록기·재생기는 P5에서 run_screen에 배선했다. **규칙·인덱스 배포는 여전히 CEO 승인 후** — `runs` 규칙과 `users.level` 규칙이 소스에만 있고 배포되지 않았다
- [x] **P5 — 새 홈 2종 + 온보딩 4장** (2026-08-23, 커밋 3개) — 피그마 와이어프레임 01~07 구현. lobby_screen 삭제, GO? 요청 동선 제거(코드는 RunService에 남음), 데모 진입점 통합, 알림 권한을 첫 완주 직후로 이동
  - 온보딩 4장 = `lib/screens/onboarding/`. 여덟 걸음만 가입 전이고 데모런부터는 로그인 뒤 — 데모런이 RunScreen(demo)을 그대로 쓰는데 그 경로가 uid를 요구한다
  - 홈 2종 = 레벨 분기(`users.level`). 새 `PacematesScreen`이 옛 홈의 친구 파트를 그대로 이어받았고(설정 → 페이스메이트), 탭바는 홈·여정·프로필
  - 세션 없는 러닝이 생겼다: `RunScreen(sessionId: '')` + `ghost`. GhostRecorder·GhostEngine 배선 완료, 종료 시 `runs`에 저장 + companions 기록. RunRecovery도 빈 sessionId를 안다
  - **뺀 것(아래 후속 과제로 등록)**: '지금 뛰는 중' 카드 · 서랍 탭 · 시드 고스트 · 러닝 화면(08~11) 재작성
- [ ] **P5.5 — 러닝 화면 재작성 (와이어프레임 08~11)** — P3·P4가 만든 층이 아직 화면에 닿지 않았다. `SessionController`(8분 에피소드 진행)·`ChartPlayer`(채보)·오디오 D안(BPM=케이던스, 레이어 게이트)을 run_screen에 배선하고, 잔향 30초(발이 결정)·전화 인터럽션 재개·화면 조작 제로를 실제 화면에서 확인할 것. **지금 '오늘의 8분 시작'은 8분 에피소드가 아니라 그냥 러닝이다** — 상태머신을 타지 않는다
  - 남은 하위: 고스트 재생 이벤트(GhostSlowdown·GhostFarewell)를 화자/사운드로 잇기 · 상대 발소리(우드 톤, 좌우 게인) · '내 발소리' 토글
- [ ] **P6 — 완주 후 5분** — 아웃트로(귀에서 착륙) → 쿨다운 디브리핑 + 음성 한 마디 채집(20초, 3회 무응답 시 빈도 하향) → 결과 화면 순서 고정(오늘의 트랙 ▶ / 함께 요약 / 여정 / 기록 접힘). 공유 카드는 기존 RepaintBoundary 경로 재활용. 응원 이월(브리지) — cheers 문서에 queued/bridged 상태
- [ ] **P7 — 첫 러닝 릴레이 + 응원 배선 + 서랍** — 신규 유저 첫 세션에 |Δspm|≤10 매칭(2분 미만 제외, 익명, Cloud Function). 응원 수신음 3종(sigHere/sigCheer/sigSlow)을 상대 리듬 박자에 삽입. 알림 다이제스트(동일인 일 3회 묶음, 죄책감 문법 금지)
  - **홈의 '지금 뛰는 중' 카드**(와이어프레임 05 두 번째 카드)가 여기 온다. P5에서 뺀 이유: 존재 레이어가 살아 있으려면 페이스메이트 문서를 실시간 구독해야 하는데(지금 friendsStream은 목록이 바뀔 때만 프로필을 다시 읽는다), 그 구독의 유일한 소비자가 응원이다. 지금 만들면 보여줄 것도 누를 것도 없는 카드가 된다
  - **서랍 탭**(와이어프레임 14) — 탭바가 셋에서 넷이 되는 시점. 화면 없는 탭을 먼저 세우지 않았다
  - **시드 고스트**(§5) — 런칭 전 실제 러닝 100개 녹음. `TodayPartnerPicker.rank(seeds:)`가 자리를 비워 두고 기다린다. 지금은 페이스메이트 고스트와 나의 과거만 후보다
- [ ] **P8 — 계측 + 베타 준비** — events 컬렉션에 북극성 계측(세션 시작/완주/부분완주, 공명 진입·누적 초, 잔향 분기, 음성 응답 여부, 릴레이 매칭·재방문, 응원 발송·브리지 재생). 대시보드는 만들지 않음. 심사 준비 항목(PrivacyInfo.xcprivacy 최초 작성 포함) 점검 + 베타 체크리스트 갱신. TestFlight 업로드는 CEO 승인 후

**전 프롬프트 공통 금지** (CLAUDE.md '금지 사항'에 반영됨): GPS 필터 값 변경 / 순위·리더보드·승패 언어·실패음·상시 배경음 / 러닝 중 화면 조작 요구 UI / firestore.rules 완화로 권한 에러 "해결" / 테스트 약화·삭제 / prototype_v2.html 수정

### P0에서 파생된 후속 과제

- [x] **데모 모드 ↔ 데모런 통합** (2026-08-23, P5) — 심사관용 진입점은 설정에 유지, 내용물은 `DemoRunScreen` 하나로 통합. '가상의 페이서예요 — 실제 사람이 아니에요'를 시작 전에 밝힌다
- [x] **라이브 동기화 v1.0 동선에서 숨기기** (2026-08-23, P5) — lobby_screen 삭제 + 홈/페이스메이트에서 GO? 발송·수신 제거. `RunService.createSession`·`pushLive`·`live` 맵과 RunScreen의 라이브 경로는 그대로 남아 있고, v1.0 동선에서 그리로 가는 문만 없다. 실기 2인 검증(§2.6)은 v1.1 과제

### 배포 대기 (CEO 승인 필요 — 밖으로 나가는 일)

- [ ] `firebase deploy --only firestore --project goingon-c12f3` — P4의 `runs` 규칙·인덱스 2개 + P5의 `users.level` 규칙. **배포 전까지 고스트 저장·조회와 레벨 저장이 실기에서 권한 거부로 실패한다**(시뮬레이터에서 빈 목록으로만 보여 조용하다). `--dry-run` 먼저

## 0. ⚠️ 푸시 알림 — 마지막 검증 한 걸음만 남음 (2026-08-14 기준)

**끝난 것** (전부 검증됨):
- Blaze 플랜, APNs 인증 키(.p8) → Firebase 업로드
- Cloud Functions 4개 배포 (`onFriendRequest`, `onFriendRequestResolved`, `onRunRequest`, `cleanupSessions`)
- App ID에 Push Notifications 활성화, `GoingOn App Store` 프로파일 재발급
- IPA 빌드 + 서명 검증 (`codesign -d --entitlements`로 `aps-environment=production` 직접 확인)
- TestFlight 업로드 + 내부 테스트 그룹 설정

**남은 것 — 이것만 하면 끝:**
- [ ] **TestFlight에 업로드** — `./tools/ship-testflight.sh` (빌드 번호 자동 증가 → IPA 빌드 → 검증 → 업로드)
      최초 1회만 App Store Connect API 키 준비 필요 — 스크립트 맨 위 주석 참조.
      키가 준비되기 전까지는 예전 방식(Xcode → Organizer ⌥⌘6 → Distribute App)도 그대로 씀
- [ ] 폰에 설치 → 앱 실행 → **알림 권한 "허용"**
- [ ] **설정 탭 → "알림" 항목이 뭐라고 쓰여 있는지 확인** ← 진단이 여기 나옴
      `이 기기로 알림을 받아요` = 성공 / `등록 실패 — …(사유)` = 괄호 안이 원인
- [ ] 토큰 확인: `cd functions && NODE_PATH=./node_modules node ../tools/push-check.js list`
- [ ] 실제 발송: `... push-check.js send <uid>` → **앱을 완전히 종료한 상태**에서 잠금화면에 뜨는지
- [ ] 되면 친구 요청/GO?로 실제 트리거도 확인 (`firebase functions:log --project goingon-c12f3`)

### 빌드 5에서 실패했던 이유 (빌드 6에서 수정됨)
`PushService.start()`가 `_registerToken()`을 `onTokenRefresh` 리스너보다 **먼저** 호출했음. iOS는 사용자가 "허용"을 누른 직후 APNs 등록이 안 끝나서 `getToken()`이 예외를 던지는데, 그러면 catch로 빠져서 **안전망 리스너가 아예 설치되지 않았음.** 그래서 토큰이 영영 저장되지 않았다(빌드 5 설치 후에도 `fcmTokens` 0대).
수정: 리스너를 먼저 걸고, APNs 준비될 때까지 1·2·3·4·5·6초 간격으로 6회 재시도. 설정 화면에 등록 상태와 실패 사유를 표시하고 탭하면 재시도하게 함(TestFlight는 로그를 볼 수 없어서 진단을 화면에 띄우는 게 유일하게 빠른 길).

### 무선 설치가 안 되는 이유
폰↔맥 페어링 정보는 **맥에 저장**되는데 랩탑을 바꿔서 사라졌음. `xcrun devicectl list devices` = 기기 0대, Apple 계정에 등록된 기기도 0대.
**케이블로 최초 1회만 연결하면** 이후 같은 Wi-Fi에서 무선 설치가 되고 사이클이 20~40분 → 2분으로 줄어듦. 남은 실기기 작업(GPS 실외, 러닝 복구)을 생각하면 케이블 하나 장만할 가치가 큼.

### 실기기 관련 제약 (중요)
**케이블이 없어서 개발용 빌드를 못 함.** 개발용 프로비저닝 프로파일은 기기 등록이 필요한데 기기를 연결할 수 없음. 따라서 **실기기 검증은 TestFlight가 유일한 경로**이고, 한 사이클이 20~40분 걸림. 시뮬레이터는 APNs 토큰 자체를 못 받아 푸시 검증이 구조적으로 불가능.

### 서명 설정 (건드리지 말 것)
- Release는 **수동 서명** + `GoingOn App Store` 프로파일. 자동 서명으로 바꾸면 아카이브 단계에서 개발용 프로파일을 요구해 **케이블 없이는 빌드가 아예 안 됨** (한 번 시도했다 되돌림)
- `aps-environment`는 빌드별로 분리: Debug/Profile → `Runner.entitlements`(development), Release → `RunnerRelease.entitlements`(production). 섞이면 앱은 정상인데 알림만 조용히 안 옴
- 같은 이름의 프로파일이 맥에 두 개 이상 설치돼 있으면 export가 엉뚱한 걸 집음. 재발급 후엔 옛것을 지울 것


작업 지시는 "TODO.md N번 해줘" 형식으로. 모든 과제는 CLAUDE.md의 **완료 기준**을 통과해야 완료이며, 완료 시 아래 "완료된 과제"로 이동.

## 0.5. 프로필 사진 — 사람 손이 필요한 확인만 남음 (2026-08-15)

프로필 편집(사진·이름·아이디) 구현 완료. Storage 활성화·규칙 배포·업로드 경로 모두 검증됨.

**끝난 것:**
- Firebase Storage 활성화 + `storage.rules`/`firestore.rules` 배포 완료
- 사진첩 접근 검증됨 — picker가 열리고 사진 목록이 보임. 권한 팝업은 뜨지 않는 게 정상
  (`requestFullMetadata: false` → PHPicker. iOS TCC에 이 앱의 사진 권한 기록 자체가 없음)
- 업로드 → `photoUrl` 저장 → 지우기 3단계 모두 실기 검증됨(시뮬레이터, 실제 계정)

**남은 것 — 사람이 직접 눌러야 확인되는 것:**
- [ ] 설정 → 프로필 편집 → 사진첩에서 실제로 한 장 골라서 아바타가 바뀌는지
- [ ] 그 사진이 홈(내 카드)·우리 탭 짝 아바타에도 반영되는지
- [ ] '사진 지우기' 후 이름 첫 글자 아바타로 돌아오는지

### ⚠️ 시뮬레이터 자동 조작은 불가능함 — 다시 시도하지 말 것 (2026-08-15 검증)
**macOS 26의 시뮬레이터는 합성 입력(synthetic event)을 일절 받지 않는다.** 아래를 전부 시도해 확인함:

| 시도 | 결과 |
|---|---|
| macOS 레벨 클릭(Apple 메뉴) | 열림 — 즉 손쉬운 사용 권한 자체는 정상 |
| `cliclick`으로 시뮬레이터 화면 좌표 클릭 | 무반응 |
| `cliclick dd:`/`du:` 분리 클릭 | 무반응 |
| AXPress로 디바이스 볼륨 버튼 누르기 | AppleScript는 성공 반환, 실제 반응 없음 |
| `osascript` 키보드 입력 | 무반응 |
| `idb` | Command Line Tools가 macOS 26 미지원 → 설치 자체가 실패 |
| 손쉬운 사용 목록에 CLI 실행파일 추가 | macOS 26에서 파일 선택창이 앱만 받아 불가 |

부수적으로 알아낸 것: 손쉬운 사용 권한은 **프로세스 시작 시점에 판정돼 캐시**되므로, 앱이 켜진 상태에서 권한을 주면 그 앱의 하위 프로세스는 재시작 전까지 계속 거부된다. (커서는 움직이는데 클릭만 안 먹는 증상)

**대안:** 화면을 눌러야 하는 검증은 (a) 사람이 직접 하거나, (b) `integration_test`로 앱 내부에서 위젯을 눌러야 함. (b)는 iOS 사진 picker 같은 Flutter 바깥 시스템 UI는 못 만지므로 플러그인 목킹이 필요함.

### 화면을 못 누를 때 쓰는 우회법 (기록)
`lib/main_*_probe.dart` 형태의 임시 엔트리포인트를 만들어 **앱 시작과 동시에 특정 경로만 실행**시키고 스크린샷으로 결과를 읽는 방법. 사진첩 접근 확인과 Storage 업로드 확인을 이걸로 했음. 확인 후 반드시 지울 것.

### 남은 심사 항목과의 연결
`NSPhotoLibraryUsageDescription`은 Info.plist에 넣었지만 `PrivacyInfo.xcprivacy`는 여전히 없음(6번 과제). 최초 작성 시 **사진 접근 + 사용자 콘텐츠(프로필 사진) 수집**을 함께 적을 것.

## 0.7. GPS 필터 수정 완료 — 실측 검증만 남음 (2026-08-15)

감사 #1·#2·#3·#4를 고치고 #9는 데이터 계층만 열었음. `flutter test` **68개 전부 통과**(빨간 상태 해소), `analyze` 에러·워닝 0, 시뮬레이터 빌드 성공.

**CLAUDE.md가 보호하는 값(정확도 30m·속도 10m/s·dt 없을 때 50m·distanceFilter 5m)은 그대로다.** 없던 필터를 켠 것이지 검증된 값을 바꾼 게 아니며, `RunFilterConfig.legacy`와 `current`가 그 네 값에서 동일한지 테스트가 지킨다.

| 감사 # | 수정 | 수정 전 → 후 (테스트 실측) |
|---|---|---|
| #1 | `maxFixAge: 10초` | 1시간 전 캐시 fix로 **5190m → 190m** |
| #2 | `minHorizontalAccuracy: 0` | 음수 정확도 fix 통과 → 거부 |
| #3 | `keepAnchorOnRejectedFix: true` + 연속 이상치 3회 탈출구 | 튐 1회에 **20m 손실 → 0** |
| #4 | `minMovementAccuracyFactor: 1` (정확도 비례) | 10분 정지 **1102m → 0m** |
| #9 | `paceSmoothingWindow: 30초` | 즉시 페이스 `null` → 계산됨 |

### ⚠️ 실기기에서 반드시 확인할 것
- [ ] **육상 트랙 400m × 10바퀴** → 4.00km ±1%. #4의 정확도 비례 계수(1.0)가 과한지 여기서 드러남
- [ ] **10분 가만히 서 있기** → 거리 증가 없어야 함. 합성 데이터로는 0이지만 실제 지터 분포는 다를 수 있음
- [ ] **러닝 중 다른 앱 10분 쓰고 복귀** → `maxFixAge: 10초`가 배경 복귀 시 정상 fix까지 버리지 않는지. 버린다면 값을 늘려야 함
- [ ] **천천히 걷기 구간** → 거리가 뭉텅이로라도 반영되는지 (기준점을 붙들기만 하고 버리지 않으므로 이론상 손실 없음)

### 알려진 트레이드오프
기준점을 붙드는 방식이라 **굽은 길에서 약간 짧게 잡힌다** — 30m를 붙들면 그 구간을 직선으로 재기 때문. 정확도가 좋으면(5m) 임계값도 5m라 영향이 미미하고, 정확도가 나쁠 때만 커진다. 신호가 나쁠 때 데이터가 거친 건 어차피 피할 수 없는 쪽이라 감수함.

### #9는 아직 화면에 안 나옴
`stats.instantSecPerKm`이 계산되지만 **RunScreen은 여전히 누적 평균을 보여준다.** 화면에 어떻게 넣을지는 프로토타입 대조가 필요한 별도 결정이라 손대지 않았음.

## 0.8. 배포 타깃 iOS 26.0으로 인상 (2026-08-16)

13.0 → **26.0**. 타깃을 얼리어답터로 좁히고 최신 API를 쓰기로 한 결정.
`ios/Runner.xcodeproj/project.pbxproj` 3곳(Debug/Release/Profile)만 수정. Podfile이 없어 SPM이라 다른 손댈 곳 없음.
IPA 내부 `MinimumOSVersion: 26.0` 확인 완료, 빌드 성공(35.1MB — 하위 호환 코드가 빠져 이전 37.7MB보다 작아짐).

- 업로드 경고 90068(2027년 봄부터 iOS 15 미만 거부) 해소됨
- **⚠️ iOS 25 이하 기기는 설치 불가.** 실기기 테스트용 아이폰이 iOS 26이어야 함

### 이제 열린 것 — GPS 감사 I-1
`HKWorkoutSession` + `HKLiveWorkoutBuilder`를 **아이폰 단독으로 쓸 수 있게 됨**(iOS 26+ 요건).
OS가 보장하는 백그라운드 실행을 얻으므로, 위치 권한만으로 버티는 현재 방식보다 기록 유실 위험이 낮다.
감사 리포트에서 "지금은 불가"로 판정했던 항목이라 재검토 대상 — 착수 전 `HKWorkoutRouteBuilder`(경로 저장, 감사 #6)와 묶어서 설계할 것.

## 0.9. GPS 검증 환경 3층 구축 (2026-08-16)

야외에 나가지 않고 거리·페이스 로직을 검증할 수 있게 됨. 전에는 "나가서 재보세요"밖에 없었다.

| 층 | 태우는 것 | 통제 가능 | 도구 |
|---|---|---|---|
| 1. 단위 테스트 | 합성 `Position` → `RunAccumulator` | 정확도·타임스탬프까지 전부 | `test/run_accumulator_test.dart` (68개) |
| 2. **시뮬레이터** | `simctl location` → geolocator → iOS 위치 스택 → 앱 | 경로·속도·간격 | `tools/sim-gps.py` + `lib/main_gps_probe.dart` |
| 3. 실기기 야외 | 진짜 GPS | 없음 | 이제 **선택** |

```bash
./tools/sim-gps.py list                       # 시나리오와 정답
./tools/sim-gps.py run straight --speed 8     # 경로 재생
./tools/sim-gps.py collect                    # 회수 + 분석
./tools/sim-gps.py gpx track400 out.gpx       # Xcode용 GPX
```

**중요 — 순서를 지킬 것:** 설치 → `simctl privacy grant location-always` → 실행. 실행 뒤에 권한을 주면 다이얼로그가 뜨고, 시뮬레이터는 탭이 안 되므로([[0.5절 참조]]) 거기서 막힌다. probe는 실행 즉시 자동 측정을 시작한다.

### 2층으로 못 보는 것
- **`horizontalAccuracy`를 조종할 수 없다** — simctl이 노출하지 않는다. 정확도 필터(감사 #2, 30m 상한)는 1층 담당
- **배경 동작을 재현할 수 없다** — 시뮬레이터는 실기기처럼 앱을 억제하지 않는다. 3층 담당

### 첫 실행에서 나온 것 (straight 1000m, 8 m/s)
수정 후가 수정 전보다 **18m 적게** 잡았는데, 그 차이가 **첫 3개 fix에서 굳은 뒤 700m 내내 정확히 그대로**였다.
출발 직후 28초 공백 뒤 fix가 몰려 들어오며 속도가 10 m/s를 넘어 이상치로 걸린 것이고, legacy는 그 18m을 그대로 더했다.
**달린 적 없는 거리이므로 버리는 게 맞다 — 감사 #3 수정이 의도대로 동작한다는 증거.**
그리고 이후 손실이 0인 것은 "기준점을 붙들면 거리가 샌다"는 우려가 실제로는 일어나지 않음을 보여준다.

### 아직 안 돌린 시나리오
- [ ] `track400` — 곡선에서 얼마나 짧게 잡히는지 (문서에 적어둔 트레이드오프의 실제 크기)
- [ ] `stationary` — 정지 드리프트, 정답 0m

## 0.95. 감사 #15 수정·검증 완료 (2026-08-16, 실기기)

배경에서 러닝 기록 스냅샷이 끊기던 문제. `lib/main_bg_probe.dart`로 실기기에서 재고, 고치고, 다시 쟀다.

| | 수정 전 | 수정 후 |
|---|---|---|
| 측정 구간 | 2분 | 3분 7초 |
| 스냅샷 저장 | 19회 | 34회 |
| **최대 공백** | **35초** | **6초** |
| 15초 초과 공백 | 1회 | 없음 |

**수정:** 타이머와 위치 콜백 **양쪽**에서 스냅샷 저장을 부르고, 간격 제어(5초)를 `_saveSnapshot` 한곳으로 모음.
배경에서는 `_seconds`가 낡은 값으로 남으므로 저장에는 항상 다시 계산한 `_elapsedSeconds`를 쓴다 — 안 그러면 배경에서 달린 시간이 짧게 기록된다.

### ⚠️ 메커니즘은 예상과 달랐다 — 기록해 둘 것
수정 후 측정에서 **위치 콜백은 130초나 비었는데도** 스냅샷은 6초 간격을 유지했다.
즉 이번에는 **타이머가 배경에서도 계속 돌았다.** "타이머가 멈추니 위치 콜백이 메워준다"는 가설과 다르다.

왜 지난번엔 35초가 비고 이번엔 안 비었는지는 **확정하지 못했다.** iOS의 배경 실행 허용 정도가 매번 다르거나(배터리·사용 이력), 화면 잠금 여부가 갈랐을 수 있다.

**그래도 수정의 가치는 유효하다.** 배경 실행 허용이 상황마다 다르다면 한 가지에만 의존하는 것이 위험하다는 뜻이고, 양쪽에 건 것이 오히려 옳은 판단이 된다.
다만 **"이 수정 덕분에 6초가 됐다"고 단정할 수는 없다** — 타이머가 이번엔 그냥 잘 돌았을 가능성이 남아 있다.

### 남은 한계
가만히 있으면서 배경에 있는 상황은 여전히 못 막는다(타이머도 위치 콜백도 안 옴). 다만 그건 달리는 중이 아니라 잃을 기록도 거의 없다.

## 1. 러닝 복구 플로우 검증 (2026-08-13 구현됨 — 검증 필요)

러닝 중 앱 강제 종료 시 기록 복구 기능이 방금 추가됨 (`run_recovery.dart`, run_screen 5초 저장, root_screen 복구 제안). 시뮬레이터에서 검증할 것:

**전제: 데모가 아닌 러닝은 상대의 준비완료가 필요하므로 혼자 재현 불가.** 시뮬레이터 2대(iPhone 16e + 아무 기종)에 테스트 계정 2개로 진행하거나, 실기기 테스트 단계(7번)로 미뤄도 됨.

- [ ] 데모 아닌 러닝 시작 → 10초 후 앱 강제 종료 → 재실행 → "마치지 못한 러닝이 있어요" 다이얼로그 확인
- [ ] "기록 저장" → FinishScreen 진입 + Firestore results 반영 확인
- [ ] "버리기" → 다이얼로그 재등장 없음 확인
- [ ] 정상 완료한 러닝 후에는 다이얼로그가 뜨지 않는 것 확인

## 2. UI 간격/폴리시 전면 정리

전 화면을 `design/prototype_v2.html`과 나란히 비교하며 패딩·간격·정렬·크기를 맞출 것. 유저 피드백: "전체적으로 간격이나 이런 것들이 깔끔하지 않다"

- [ ] 화면별로 스크린샷 → 프로토타입 비교 → 차이 목록 보고 → 수정 순서로 진행

## 2.5. 공명 이벤트 레이어 (2026-08-16 — 소켓만 완료)

`lib/services/resonance.dart` — 연속값(closeness)에서 **순간**을 뽑아 UI·사운드·햅틱이 함께 구독할 단일 스트림으로 낸다. 판정을 세 곳에서 각자 하면 화면은 반짝였는데 소리는 안 나는 어긋남이 생기므로, 판정은 엔진 한 곳에만 둔다.

- [x] 상태 4개(drifting/approaching/aligned/resonant) + **히스테리시스**(진입≠이탈). 문턱 하나면 경계에서 초당 수십 번 튀고, 소리가 붙으면 진입음이 그만큼 울린다
- [x] 지수 스무딩 — 시정수 `kSharedSmoothingTimeConstant`(1초). **오디오 스무딩도 반드시 이 값**이어야 소리와 화면이 같은 속도로 붙었다 떨어진다
- [x] 데모 파트너 지수의 타임라인(`demo_resonance.dart`) — 약 19초·62초·137초에 공명, 마지막은 60초 넘게 유지
- [x] 확인 도구 `lib/main_resonance_probe.dart` — 데모 러닝 화면을 손 안 대고 띄운다. 성능 오버레이는 `--dart-define=perf=true`
- [x] **러닝 화면이 이 레이어를 소비함** (2026-08-16) — 겹침 = `smoothedCloseness`, 공명 진입 = 골드 링 3겹 + 겹침 플래시 + 햅틱, 30초 이상 유지 = 겹침이 아주 느리게 숨쉼, 상태 전이 = 상태어 크로스페이드

- [x] **신호 피드백 루프**(2026-08-16) — 신호를 셋으로 줄이고(탭 "여기 있어" / 스와이프 "힘내" / 길게 "천천히 가자") 송신 잔상·수신 맥동·햅틱·데모 응답·10초 쿨다운을 붙임. 프로토타입의 넷(파이팅·보고싶어·빨라지자·천천히)과 다르다 — 달리면서 넷은 기억 못 한다는 판단

- [x] **사운드 엔진 + 공명 사운드**(2026-08-16) — `docs/sound_ux_v1.md` 프롬프트 5. 오디오 세션(playback+mixWithOthers, 러닝 중에만 활성), `SoundEngine` 인터페이스 + flutter_soloud 구현, 공명 패드(0.70~1.00 → 볼륨 0~1)·chime·30초 배음, 설정 토글

남은 것:

- [ ] **음원 교체** — 지금 `assets/audio/`에 든 셋은 내가 합성한 **임시 음원**이다. 설계서가 말하는 `goingon_sound_proto` 산출물이 저장소에 없어 같은 규격(모노 44.1k / 8초 심리스 루프 / 피크 -6dBFS)으로 만들어 넣었다. 최종 음원은 -14 LUFS 기준으로 맞춰 교체할 것
- [ ] **실기 청감 확인** — 시뮬레이터는 소리를 들려주지 못해 "무엇이 언제 울렸는가"까지만 로그로 확인했다. 음악(스포티파이) 위에 얹었을 때 튀지 않는지, 패드가 거슬리지 않는지는 귀로만 판단된다
- [x] **신호음 + 1km 브리핑**(2026-08-16) — 설계서 프롬프트 6. 송신 0.4 / 수신 3종 0.8(음높이·타수·길이가 모두 다름), TTS 한국어 브리핑, 발화 중 덕킹·이벤트음 폐기, 설정 토글 독립
- [ ] **브리핑 실기 청감 확인** — 길이는 로그로 쟀지만(3.4초/2.6초) "5분 32초"·"3.4킬로"를 자연스럽게 읽는지는 귀가 필요하다. 어색하면 숫자를 한글로 바꿔 넘기는 방식과 비교할 것(`BriefingScript`만 고치면 된다)
- [ ] **스포티파이 덕킹 확인** — 음악이 낮아졌다 1초 뒤 돌아오는지. 시뮬레이터에는 음악 앱이 없다
- [ ] **햅틱만으로 세 신호가 구분되는지 실기 확인** — 시뮬레이터는 진동하지 않아 사람 손이 필요하다. 화면을 가리고 데모를 돌려 한 번/두 번/길게가 구분되는지 볼 것 (구분을 세기가 아니라 **박자**에 걸어둔 이유도 이것)
- [ ] 사운드를 이 스트림에 연결 (구독자가 0명이어도 동작이 같도록 broadcast로 만들어 둠)
- [ ] **러닝 화면의 골드 숫자를 무엇으로 할지** — 프롬프트에서 "공명 진입 횟수라면 링과 연결하라"고 했으나, 실제 세션에는 발맞춤 값이 없어 횟수가 영영 0이다. 그래서 이번엔 kcal을 러닝 화면에서 **빼고**(완료 화면에 이미 있음) 골드를 공명 전용 색으로 남겼다. 라이브 동기화가 생기면 그때 공명 횟수를 올릴 자리
- [ ] **실세션 closeness — 라이브 동기화로 푸는 중** (2026-08-17 결정 변경). 케이던스를 실시간으로 주고받아 공명을 실제로 작동시킨다. 진행 상황은 아래 '## 2.6 라이브 동기화' 참조

## 2.6. 라이브 동기화 — 실세션 공명 (2026-08-17)

CLAUDE.md의 "러닝 중 실시간 동기화 없음"을 폐기하고 당긴 작업. 케이던스를
주고받아 **실제 러닝에서 공명이 작동**한다.

- [x] 케이던스 측정 — `CadencePlugin.swift`(CMPedometer) + `CadenceService`.
      pub.dev 플러그인들은 걸음 *수*만 줘서 직접 붙였다
- [x] `live.{uid}` 맵 — 3초 스로틀 + 유의미한 변화만(`LiveWriteGate`).
      규칙은 results와 같은 패턴으로 자기 칸만 쓰기
- [x] closeness 주입 — 케이던스 차 0~30spm → 1.0~0.0(`CadenceCloseness`).
      6초 넘은 값은 흘려 넣지 않는다
- [x] 동심원 재설계 — 두 원이 같은 중심, 반지름은 각자의 케이던스

**실기 2인 검증이 남았다 (시뮬레이터로는 불가):**
- [ ] 두 사람이 실제로 달리며 상대 원이 내 화면에서 움직이는지
- [ ] 발을 맞췄을 때 공명(골드·종소리·햅틱)이 실제로 뜨는지
- [ ] 한 명이 앱을 배경으로 내려도 상대 화면에서 사라지지 않는지
      (위치 콜백 경유 쓰기가 실제로 이어지는지)
- [ ] Always 권한 없이 화면 유지 모드일 때도 같은지
- [ ] 30분 러닝 후 Firestore 쓰기 횟수 확인 — 인당 600회 이하 예상

## 3. 햅틱 확장

현재 롱프레스 제스처(보고싶어)에만 적용됨. 확장 — 위 공명 스트림을 구독하는 형태로:

- [ ] 로비 카운트다운(3-2-1: light, 출발: heavy), 러닝 종료 확정, 제스처 수신 시
- [ ] 과하지 않게 — 각 순간에 한 번, 패턴은 순간의 무게에 비례

## 4. Live Activities (잠금화면 / Dynamic Island)

- [ ] `live_activities` 패키지 + Swift 위젯 익스텐션 추가 (Xcode에서 타겟 생성 필요 — 절차를 단계별로 안내할 것)
- [ ] 표시 데이터: 경과 시간, 내 거리, 상대 이름, 앱 컬러(lime/coral) 반영
- [ ] 러닝 시작 시 시작, 종료/취소 시 반드시 종료 처리 (좀비 Live Activity 방지)

## 5. 기술 부채 (TECH_REVIEW.md 2026-07-12 지적 사항 중 잔여)

- [x] ~~`withOpacity` → `withValues` 일괄 치환~~ — 2026-08-16 완료. 실제로는 73곳이었고 인자가 전부 숫자 리터럴이라 기계적 치환이 안전했음.
      `flutter analyze` 123건 → **50건** (deprecated 73건 전부 소멸). 알파값은 그대로라 화면 변화 없음(브랜드 마크의 반투명 겹침으로 확인)
- [x] ~~friendsStream 이중 구독 정리~~ — 2026-08-16 완료. RootScreen으로 끌어올리는 대신 **서비스 층에서 원본을 공유**하는 방식(`shared_stream.dart`)을 택함.
      홈의 재시도·마지막 목록 유지 로직을 옮기지 않아도 되고, 순수 Dart라 "구독자 둘 → 원본 하나"를 테스트로 증명할 수 있음(5개)
- [x] ~~세션 상태 전이 유닛 테스트~~ — 2026-08-16 완료. `fake_cloud_firestore`를 들이는 대신 `session_rules.dart`로 **순수 로직을 분리**함(`RunAccumulator`와 같은 방식).
      트랜잭션 껍데기는 `run_service`에 남고 판정만 떼어냈으므로 동작은 그대로. 테스트 22개.
      전이 자체보다 **하지 말아야 할 것**을 주로 검증함 — 취소된 세션 부활 금지, `startedAt` 덮어쓰기 금지, 재제출 시 이중 집계 금지.
      부수 효과로 `kRequestTtl`이 두 곳에 정의돼 갈릴 수 있던 것을 `SessionRules.requestTtl` 하나로 묶음

## 6. 심사 준비

- [x] ~~차단~~ — 요청·수락 모델과 함께 구현됨(홈 롱프레스 → 차단, 설정 → 차단 목록에서 해제)
- [ ] **설정에 신고 연락처(이메일) 노출** — 차단은 끝났고 신고 경로만 남음. 참고: 유저 간 자유 입력 텍스트는 이름·아이디뿐이고 GO? 거절 메시지는 3개 중 선택식이라, 가이드라인 1.2의 초점은 유해 텍스트 필터링이 아니라 **동의 없는 연락 차단**임(그건 요청·수락 + 차단으로 대응됨)
- [ ] `PrivacyInfo.xcprivacy` 작성 (위치·유저 데이터 수집 명시)
      **모션 사유를 반드시 포함할 것** — 2026-08-17에 케이던스(CMPedometer)를 붙이며
      `NSMotionUsageDescription`은 넣었지만 xcprivacy는 아직 없다. 작성 시
      사진 접근·사용자 콘텐츠와 함께 **모션/피트니스**를 한 번에 적는다
- [ ] 위치 권한 설명 문구(NSLocationAlwaysAndWhenInUseUsageDescription 등) 심사 기준으로 재작성
- [ ] 심사관용 데모 모드 안내를 App Review 노트에 쓸 문구 초안
- [ ] 두 명이 실제로 함께 달리는 30초 시연 영상 촬영(실전 테스트 때 화면 녹화) → App Review 노트에 링크 첨부

## 7. 출시 검증

- [ ] 실기기 GPS 테스트 (실외 1km 이상, 폰 잠근 상태 + 강제 종료 복구 포함)
- [ ] 친구 1명과 실전 테스트
- [ ] TestFlight 배포

---

## 완료된 과제

- [x] 우리 탭 구현 (us_screen.dart)
- [x] 설정 탭 구현 (settings_screen.dart)
- [x] Crashlytics 도입 (main.dart 삼중 캐치 + 전 서비스 recordError)
- [x] 러닝 복구 구현 (run_recovery.dart — 검증은 1번 과제)
- [x] 2026-08-13 안정성 감사: 시간 계산(타임스탬프 차이 ✓), 집계(트랜잭션+증분 ✓), GPS 필터(30m/10m·s ✓) — 문제 없음 확인
- [x] 폰트 번들 전환(런타임 다운로드 제거) + `flutter analyze` 정상화(8,536→124, build/ 제외)
- [x] 데모 복구 버그 — 데모 러닝이 RunRecovery에 저장돼 심사관이 무한 다이얼로그를 밟을 수 있던 문제. 저장·복구 양쪽 가드 + 회귀 테스트 6개
- [x] **친구 연결을 '요청 → 수락' 모델로 전환** + 차단. 보안 규칙에서 friends 직접 쓰기를 봉쇄함

## 검증이 남은 것 (2인 필요 — 실기기/시뮬레이터 2대)

요청·수락 흐름은 계정 2개가 있어야 끝까지 볼 수 있음. 7번 실전 테스트 때 함께 확인:
- [ ] A가 요청 → B 홈 상단에 "나에게 온 요청" 카드 → 수락 → 양쪽 친구 목록에 반영
- [ ] 거절이 조용히 처리되는지(A에게 알림 없음), A가 재요청 가능한지
- [ ] 차단 후 상대가 나를 검색하면 "그런 아이디를 쓰는 사람이 없어요"가 나오는지
- [ ] 차단 해제 후 다시 요청→수락으로 연결되는지
